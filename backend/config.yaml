# StockTracker Configuration
# ===========================

# Database settings
database:
  # SQLite database file path (relative to backend/ directory)
  url: "sqlite+aiosqlite:///./stocktracker.db"

# Background scheduler settings
scheduler:
  # How often to refresh stock data (in seconds)
  # Recommended: 30-300 for normal use; minimum 10 to avoid API throttling
  refresh_interval_seconds: 30

  # Only fetch data during market trading hours?
  # When enabled, each market is checked independently:
  #   CN: 9:30-15:00 CST  |  US: 9:30-16:00 ET
  # Markets outside trading hours are skipped; open markets still refresh.
  market_hours_only: false

# CORS settings (allowed origins for frontend dev server)
cors:
  origins:
    - "http://localhost:5173"
    - "http://127.0.0.1:5173"

# Server settings
server:
  host: "0.0.0.0"
  port: 8000
